// empty javascript file

const testObject = {
    name: "test object",
    color: "green",
    numericProperty: {
        one: 1,
        two: 2,
    },
    bool: false,
}

const testFunction = () => {
    let a = 0;
    a++
    return a
}
